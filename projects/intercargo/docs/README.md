# Intercargo project documentation

Current workspace/development architecture is documented at the repository root under `docs/`.

Historical Intercargo implementation and release notes are retained under `legacy/` for reference. Paths mentioned in those older documents may describe the pre-workspace layout; current source lives under `projects/intercargo/`.
